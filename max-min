#include <iostream>
using namespace std;

int main() {
    int A, B, C;
    cin >> A >> B >> C;

    int minimum = min(A, min(B, C));
    int maximum = max(A, max(B, C));

    cout << minimum << " " << maximum << endl;

    return 0;
}
